/* type Platform = 'Windows' | 'Mac OS' | 'Linux';
type Feature = 'Single Player' | 'Multiplayer' | 'Co-op';

interface GameDetails {
    id: string,
    title: string,
    description: string,
    platforms: Platform[],
    features: Feature[]
} */

let message: string = 'Laura Carlota';

console.log(message);
